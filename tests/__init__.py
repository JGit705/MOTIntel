"""Tests. Run from the repository root as modules, e.g.

    python -m tests.test_app_smoke
    python -m tests.test_llm_grounding

Running them as modules puts the repository root on sys.path, so the package
imports without an install step and without any sys.path manipulation.
"""
